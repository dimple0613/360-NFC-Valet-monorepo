{
  "name": "360-nfc-valet-qae2e",
  "version": "1.0.0",
  "private": true,
  "description": "Full website end-to-end Playwright QA suite for the 360 NFC Valet monorepo (Super Admin, Tenant Admin, guest web).",
  "scripts": {
    "install-browsers": "npx playwright install chromium",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.14.0"
  }
}
